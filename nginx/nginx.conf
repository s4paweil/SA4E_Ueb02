events {}

http {
    upstream backend {
        server makeAWish-service1:3000;
        server makeAWish-service2:3000;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://backend;
        }
    }
}
